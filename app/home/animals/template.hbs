
<div class="col-md-10 col-md-offset-1 mb50">
  <section class="row">

  {{#each animals as | animal | }}
    <div class="col-md-4">
      <figure>
        <div class="circle-image">
          <img src="/assets/images/{{animal.name}}.png" alt="{{animal.name}}">
        </div>
        <figcaption>
          <h2>{{animal.name}}</h2>
          <h2>£{{animal.price}}</h2>
        </figcaption>
      </figure>
    </div>
  {{/each}}

  </section>
</div>
